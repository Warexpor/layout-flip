# Prefer Omarchy Lua binds (share/hyprland/bindings.example.lua).
# Select text, then Ctrl+Shift+X → retypex sel.
# Use press bind (not bindr): release order of Ctrl/Shift/X is unreliable.
bind = CTRL SHIFT, code:53, exec, retypex sel
