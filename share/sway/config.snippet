# Prefer retypex on Hyprland. For Sway, selection-only fallback:
# bindsym --release Ctrl+Shift+x exec layout-flip --clip
# (in-app replace needs a compositor-specific injector; use retypex/gswitch where possible)
bindsym --release Ctrl+Shift+x exec layout-flip --clip
